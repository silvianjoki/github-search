import { VistedrepoDirective } from './vistedrepo.directive';

describe('VistedrepoDirective', () => {
  it('should create an instance', () => {
    const directive = new VistedrepoDirective();
    expect(directive).toBeTruthy();
  });
});
